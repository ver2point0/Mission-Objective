<%= form_for [user, item] do |f| %>
    <div class="form-group">
        <%= f.label :name %> <br />
        <%= f.text_field :name, class: 'form-control', placeholder: "Enter a new objective name" %>
    </div>
    <div class="actions">
        <%= f.submit "Add objective", class: 'btn btn-success btn-block' %>
    </div>
<% end %>
